'use strict';

angular.module('stepApp')
    .factory('Lang', function ($resource, DateUtils) {
        return $resource('api/langs/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });